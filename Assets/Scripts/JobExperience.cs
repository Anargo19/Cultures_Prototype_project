using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JobExperience : MonoBehaviour
{
    public int ExperienceStoneGatherer;
    public int ExperienceWoodGatherer;
    public int ExperienceClayGatherer;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
