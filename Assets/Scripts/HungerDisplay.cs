using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HungerDisplay : MonoBehaviour
{
    public float hunger;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("OverHunger");
        //gameObject.transform.parent.parent.parent.parent.gameObject.GetComponent<ObjectInfo>().DisplayHunger();
        //hunger = gameObject.transform.parent.parent.parent.parent.gameObject.GetComponent<ObjectInfo>().
    }

    private void OnMouseOver()
    {
        
    }
}
